# V63 後台安全登入設定

## 新增 Google 試算表頁籤

執行一次 `setup()` 後會自動建立：

- 最新消息
- 客戶表單
- 常見問題
- 公司資訊
- 管理人員
- 管理權限
- 驗證紀錄

## 管理人員

「管理人員」不保存明文密碼，欄位包含：

- 管理員ID
- 姓名
- 帳號
- 密碼鹽值
- 密碼雜湊
- 啟用
- 權限代碼
- 更新時間

前端不會讀回這個頁籤內容。

## 驗證紀錄

只記錄：

- 驗證時間
- 帳號
- 驗證結果 TRUE / FALSE
- 備註

不會把使用者輸入的密碼寫進試算表。

## 首次建立管理員

`setup()` 不會建立任何固定預設密碼。

第一次設定時，請在 Apps Script 手動執行一次：

`createInitialAdmin()`

系統會產生：

- 帳號：`admin`
- 一組隨機 16 碼一次性初始密碼

請到 Apps Script 的「執行記錄 / Logs」查看一次性密碼。
登入後請立即到「管理員管理」變更密碼。

因此網站程式碼與 GitHub 檔案中不會存在固定的預設管理密碼。

## Apps Script 部署

修改 Code.gs 後：

1. 儲存
2. 執行 `setup()` 一次
3. 部署 → 管理部署作業
4. 編輯
5. 選「新版本」
6. 部署
7. 保持原本 `/exec` URL

## 後台入口

前台已完全移除「後台入口」與後台登入彈窗。

管理者需自行輸入：

https://chuang-c.com/admin/

直接進入 `/admin/` 也只會先看到登入頁。

## F12

已加入 F12、Ctrl+Shift+I/J/C、Ctrl+U、右鍵的基本阻擋。

但 GitHub Pages 是公開靜態網站，任何前端 HTML / CSS / JS 都無法做到真正不可取得。
真正的安全措施是：

- 前端不保存管理員密碼
- 密碼雜湊只存在 Google 試算表
- 管理 API 全部需要 Session Token
- 管理員資料不會回傳到瀏覽器
- Token 逾時失效
